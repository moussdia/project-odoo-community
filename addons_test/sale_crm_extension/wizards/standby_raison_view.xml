<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
         <record id="create_standby_raison_wizard" model="ir.ui.view">
            <field name="name">standby.raison.wizards</field>
            <field name="model">standby.raison</field>
            <field name="arch" type="xml">
                <form string="Motif">
                    <group string="Motif">
                        <field name="note" nolabel="1" placeholder="Motif"/>
                    </group>
                    <footer>
                        <button name="action_standby_reason_apply" string="Mettre en stand-by" type="object" class="oe_highlight"/>
                        <button special="cancel" string="Annuler" type="object" class="btn-secondary"/>
                    </footer>
                </form>
            </field>
        </record>
        <record id="action_standby_raison_wizard" model="ir.actions.act_window">
            <field name="name">Motif</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">standby.raison</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>

    </data>
</odoo>

