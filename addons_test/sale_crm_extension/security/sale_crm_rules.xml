<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- rule to see all campaign by Chief Service -->
        <!--<record model="ir.rule" id="validate_campaign_chief_service_commercial_rule">
            <field name="name">Voir les campagnes à l'étape fait</field>
            <field name="model_id" ref="model_utm_campaign"/>
            <field name="groups" eval="[(4, ref('sale_crm_extension.group_chef_service_commercial'))]"/>
            <field name="domain_force">[('state', '=', 'done')]</field>
        </record>-->

        <!-- rule delete contract if state not in in_progress or expired -->
        <!--<record model="ir.rule" id="sale_crm_extension_group_head_account_delete_rule">
            <field name="name">delete.contract.group_head_account</field>
            <field name="model_id" ref="model_sale_contract"/>
            <field name="groups" eval="[(4,ref('sale_crm_extension.group_head_account'))]"/>
            <field name="domain_force">[('state', '=', 'draft')]</field>
            <field eval="1" name="perm_unlink"/>
            <field eval="1" name="active"/>
        </record>-->

        <!-- rule to validate quotation by Chief Service -->
        <!--<record model="ir.rule" id="validate_quotation_chief_service_rule">
            <field name="name">Valider le devis de son equipe</field>
            <field name="model_id" ref="model_sale_order"/>
            <field name="domain_force">[('employee_id.department_id.manager_id.user_id.id', '=', user.id),('state', '=',
                'submitted')]
            </field>
            <field eval="1" name="perm_write"/>
            <field eval="0" name="active"/>
        </record>-->

    </data>
</odoo>
