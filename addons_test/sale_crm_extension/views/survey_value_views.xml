<odoo>
    <data>
    <!--ADD THEME Action Menu Tree view -->
        <record id="utm_value_tree_view" model="ir.ui.view">
			<field name="name">utm.value.tree</field>
			<field name="model">utm.value</field>
			<field name="type">tree</field>
			<field name="arch" type="xml">
				<tree string="Valeur" editable="bottom" default_order="id desc">
					<field name="name" required="1"/>
                    <field name="description"/>
				</tree>
			</field>
		</record>

        <!-- Action Menu Item Related To Above Action Window -->
        <record id="action_utm_value_menu" model="ir.actions.act_window">
            <field name="name">Valeur</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">utm.value</field>
            <field name="view_mode">tree,form</field>
            <field name="help" type="html">
                <p>
                    Valeur
                </p>
            </field>
        </record>

         <menuitem id="utm_value_menu"
                  name="Valeur"
                  parent="survey_configuration_menu"
                  action="action_utm_value_menu"
                  sequence="35"
        />

    </data>
</odoo>
