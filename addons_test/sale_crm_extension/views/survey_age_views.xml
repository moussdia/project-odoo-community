<odoo>
    <data>
    <!--ADD THEME Action Menu Tree view -->
        <record id="utm_age_tree_view" model="ir.ui.view">
			<field name="name">utm.age.tree</field>
			<field name="model">utm.age</field>
			<field name="type">tree</field>
			<field name="arch" type="xml">
				<tree string="Age" editable="bottom" default_order="id desc">
					<field name="name" required="1"/>
                    <field name="description"/>
				</tree>
			</field>
		</record>

        <!-- Action Menu Item Related To Above Action Window -->
        <record id="action_utm_age_menu" model="ir.actions.act_window">
            <field name="name">Age</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">utm.age</field>
            <field name="view_mode">tree,form</field>
            <field name="help" type="html">
                <p>
                    Age
                </p>
            </field>
        </record>

         <menuitem id="utm_age_menu"
                  name="Age"
                  parent="survey_configuration_menu"
                  action="action_utm_age_menu"
                  sequence="50"
        />

    </data>
</odoo>
